function validate(event){
    if (document.studentDetails.age.value == '-1') {
        event.preventDefault();
    }

    if (document.studentDetails.age.value== '-1') {
        event.preventDefault();
    
    }
}
